.ui.container.main-container.text
  %h1.ui.header.dividing= @post.header
  %i= @post.info
  %p= @post.body.html_safe
  - if @post.user == current_user
    = link_to "Edit Post", edit_post_path(@post), class: "ui button primary"